import { UnspentTransaction } from './unspent-transaction';

describe('UnspentTransaction', () => {
  it('should create an instance', () => {
    expect(new UnspentTransaction()).toBeTruthy();
  });
});
