import { LitedogeBufferwriter } from './litedoge-bufferwriter';

describe('LitedogeBufferwriter', () => {
  it('should create an instance', () => {
    expect(new LitedogeBufferwriter()).toBeTruthy();
  });
});
