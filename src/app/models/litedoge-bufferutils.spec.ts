import { LitedogeBufferutils } from './litedoge-bufferutils';

describe('LitedogeBufferutils', () => {
  it('should create an instance', () => {
    expect(new LitedogeBufferutils()).toBeTruthy();
  });
});
